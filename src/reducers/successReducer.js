const initialState = {}

const successReducer =  (state = initialState, action) => {
  switch (action.type) {
    case 'value':
      
      break;
  
    default:
      return state;
  }
};

export default successReducer;